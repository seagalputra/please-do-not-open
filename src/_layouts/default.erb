<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: resource.data.title %>
  </head>
  <body class="flex flex-col min-h-screen bg-neutral-50 break-words">
    <%= render "navbar", metadata: site.metadata %>

    <main class="px-6 lg:px-20 grow py-6 mt-[106px]">
      <%= yield %>
    </main>

    <%= render "footer", metadata: site.metadata %>
  </body>
</html>
